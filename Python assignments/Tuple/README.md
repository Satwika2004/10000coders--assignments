**#TUPLE BUILT IN FUNCTIONS**



This folder demonstrates the use of tuple operations, manual implementations of built-in functions.
#problems
1. Car Parking Bill Calculation
2. Manual Implementations of Tuple Built-in Functions (`count()`, `index()`, `len()`, `all()`)
